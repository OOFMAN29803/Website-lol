    const element1 = document.getElementById('productDiv');
    const element2 = document.getElementById('productDiv2');
    const element3 = document.getElementById('productDiv3');
    const element4 = document.getElementById('productDiv4');
    const element5 = document.getElementById('productDiv5');
    const element6 = document.getElementById('productDiv6');
    const element7 = document.getElementById('productDiv7');
	var hoverElement = document.getElementById("elementHoverParent")
	var informationText = document.getElementById("infoText")
    
	element1.addEventListener('mouseenter', () => {
      hoverElement.style.display ="block";
	  infoText.textContent = "An IDE that is a run on device and a on server system that replicates a computer without a live system!"
	  
    });
	

    // Add a mousemove event to the document
    document.addEventListener('mousemove', (event) => {
      // Get the mouse coordinates
      const mouseX = event.clientX;
      const mouseY = event.clientY;

      hoverElement.style.left = `${mouseX}px`
	  hoverElement.style.top = ` ${mouseY}px`
    });

    element1.addEventListener('mouseleave', () => {
      hoverElement.style.display ="none";

    });
	
	element2.addEventListener('mouseenter', () => {
      hoverElement.style.display ="block";
	  infoText.textContent = "A cheap future AI subscription that provides limited messages of the new top tier AI model from AllOneLM"
	  
    });
	

    // Add a mousemove event to the document
    document.addEventListener('mousemove', (event) => {
      // Get the mouse coordinates
      const mouseX = event.clientX;
      const mouseY = event.clientY;

      hoverElement.style.left = `${mouseX}px`
	  hoverElement.style.top = ` ${mouseY}px`
    });

    element2.addEventListener('mouseleave', () => {
      hoverElement.style.display ="none";

    });
	
		element3.addEventListener('mouseenter', () => {
      hoverElement.style.display ="block";
	  infoText.textContent = "A cheap future AI subscription that provides almost unlimited messages from top tier models from AllOneLM."
	  
    });
	

    // Add a mousemove event to the document
    document.addEventListener('mousemove', (event) => {
      // Get the mouse coordinates
      const mouseX = event.clientX;
      const mouseY = event.clientY;

      hoverElement.style.left = `${mouseX}px`
	  hoverElement.style.top = ` ${mouseY}px`
    });

    element3.addEventListener('mouseleave', () => {
      hoverElement.style.display ="none";

    });
	
			element4.addEventListener('mouseenter', () => {
      hoverElement.style.display ="block";
	  infoText.innerHTML = "A bundle subscription that is pioneered for everything in mind."+ "<br><br>" + "- %VIDEOLESSADS%. " + "<br>" + "- 25GB of Storage. " + "<br>" + "- Very limited AI messages (Less than Basic AI subscription)"
	  
    });
	

    // Add a mousemove event to the document
    document.addEventListener('mousemove', (event) => {
      // Get the mouse coordinates
      const mouseX = event.clientX;
      const mouseY = event.clientY;

      hoverElement.style.left = `${mouseX}px`
	  hoverElement.style.top = ` ${mouseY}px`
    });

    element4.addEventListener('mouseleave', () => {
      hoverElement.style.display ="none";
	    element4.removeEventListener('mousemove', () => {});


    });
	
				element5.addEventListener('mouseenter', () => {
      hoverElement.style.display ="block";
	  infoText.innerHTML = "A bundle subscription that is pioneered for everything in mind."+ "<br><br>" + "- %VIDEOLESSADS%. " + "<br>" + "- 100GB of Storage. " + "<br>" + "- Basic AI Subscriptions messages " + "<br>" + " -%SECURITY SUITE BASIC%"
	  
    });
	

    // Add a mousemove event to the document
    document.addEventListener('mousemove', (event) => {
      // Get the mouse coordinates
      const mouseX = event.clientX;
      const mouseY = event.clientY;

      hoverElement.style.left = `${mouseX}px`
	  hoverElement.style.top = ` ${mouseY}px`
    });

    element6.addEventListener('mouseleave', () => {
      hoverElement.style.display ="none";
	    element6.removeEventListener('mousemove', () => {});


    });
	
					element6.addEventListener('mouseenter', () => {
      hoverElement.style.display ="block";
	  infoText.innerHTML = "A bundle subscription that is pioneered for everything in mind."+ "<br><br>" + "- %VIDEONOADS%. " + "<br>" + "- 100GB of Storage. " + "<br>" + "- Basic AI Subscriptions messages " + "<br>" + " -%SECURITY SUITE FULL% " + "<br>" + "-AllOneIDE Subscription"
	  
    });
	

    // Add a mousemove event to the document
    document.addEventListener('mousemove', (event) => {
      // Get the mouse coordinates
      const mouseX = event.clientX;
      const mouseY = event.clientY;

      hoverElement.style.left = `${mouseX}px`
	  hoverElement.style.top = ` ${mouseY}px`
    });

    element6.addEventListener('mouseleave', () => {
      hoverElement.style.display ="none";
	    element6.removeEventListener('mousemove', () => {});


    });
	
					element7.addEventListener('mouseenter', () => {
      hoverElement.style.display ="block";
	  infoText.innerHTML = "A bundle subscription that is pioneered for everything in mind."+ "<br><br>" + "- %VIDEONOADS%. " + "<br>" + "- 250GB of Storage. " + "<br>" + "- Almost Unlimited AI Subscriptions messages " + "<br>" + " -%SECURITY SUITE FULL% " + "<br>" + "-AllOneIDE Subscription" + "<br>" + "Early preview on exclusive features."
	  
    });
	

    // Add a mousemove event to the document
    document.addEventListener('mousemove', (event) => {
      // Get the mouse coordinates
      const mouseX = event.clientX;
      const mouseY = event.clientY;

      hoverElement.style.left = `${mouseX}px`
	  hoverElement.style.top = ` ${mouseY}px`
    });

    element7.addEventListener('mouseleave', () => {
      hoverElement.style.display ="none";
	    element7.removeEventListener('mousemove', () => {});


    });